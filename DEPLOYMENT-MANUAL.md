# РУКОВОДСТВО ПО РАЗВЕРТЫВАНИЮ И БЕЗОПАСНОСТИ
## Сайт швейной мастерской "Ulla"

---

**Версия документа:** 1.0  
**Дата:** 11 января 2026  
**Автор:** GitHub Copilot  
**Статус:** Финальная версия

---

## РАЗДЕЛ 1. ВВЕДЕНИЕ

### 1.1 О системе

Данное руководство описывает процесс развертывания и обеспечения безопасности веб-сайта швейной мастерской "Ulla". Система представляет собой статический веб-сайт с административной панелью для управления контентом.

**Технические характеристики:**
- Тип: Статический веб-сайт
- Технологии: HTML5, CSS3, JavaScript (ES6+)
- Хранение данных: LocalStorage браузера
- Языки интерфейса: Латышский, Русский
- Адаптивность: Desktop, Tablet, Mobile

**Компоненты системы:**
- Главная страница (index.html)
- Административная панель (admin.html)
- Визуальный редактор (visual-editor.html)
- JavaScript модули (app.js, admin.js, visual-editor.js)
- Таблицы стилей (styles.css, admin-styles.css)

### 1.2 Требования к системе

**Минимальные требования:**
- Веб-сервер с поддержкой статических файлов
- Современный браузер (Chrome 90+, Firefox 88+, Safari 14+)
- Интернет-соединение для загрузки внешних библиотек

**Рекомендуемые требования для сервера:**
- ОС: Ubuntu 20.04 LTS или выше
- RAM: минимум 512 MB
- Disk: минимум 100 MB свободного места
- Веб-сервер: Nginx 1.18+ или Apache 2.4+

---

## РАЗДЕЛ 2. ВАРИАНТЫ РАЗВЕРТЫВАНИЯ

### 2.1 GitHub Pages (бесплатно, рекомендуется)

**Преимущества:**
- Полностью бесплатный хостинг
- Автоматическое HTTPS
- Интеграция с Git
- 100 GB месячного трафика
- Автоматическое развертывание при коммите

**Пошаговая инструкция:**

**Этап 1: Регистрация на GitHub**
1. Откройте браузер и перейдите на https://github.com
2. Нажмите кнопку "Sign up" в правом верхнем углу
3. Заполните регистрационную форму:
   - Username: выберите уникальное имя пользователя
   - Email address: введите действующий email
   - Password: создайте надежный пароль (минимум 8 символов)
4. Пройдите проверку CAPTCHA
5. Подтвердите email адрес, перейдя по ссылке из письма

**Этап 2: Создание репозитория**
1. После входа в аккаунт нажмите "+" в правом верхнем углу
2. Выберите "New repository"
3. Заполните данные репозитория:
   ```
   Repository name: sewing
   Description: Веб-сайт швейной мастерской Ulla
   Visibility: Public (обязательно для бесплатного хостинга)
   ```
4. НЕ выбирайте опции:
   - Add a README file
   - Add .gitignore
   - Choose a license
5. Нажмите "Create repository"

**Этап 3: Загрузка файлов (способ для начинающих)**
1. На странице нового репозитория найдите текст "uploading an existing file"
2. Кликните по этой ссылке
3. Откроется окно загрузки файлов
4. Перетащите следующие файлы в окно браузера:
   - index.html
   - admin.html
   - visual-editor.html
   - app.js
   - admin.js
   - visual-editor.js
   - styles.css
   - admin-styles.css
   - logo-ulla.jpg
   - README.md
   - DEPLOYMENT-GUIDE.md
   - И все остальные файлы проекта
5. В поле "Commit message" введите: "Initial commit - добавление файлов сайта"
6. Нажмите зеленую кнопку "Commit changes"
7. Дождитесь завершения загрузки

**Этап 4: Активация GitHub Pages**
1. В репозитории перейдите во вкладку "Settings"
2. В левом меню найдите раздел "Pages"
3. В секции "Build and deployment":
   - Source: Deploy from a branch
   - Branch: выберите "main"
   - Folder: выберите "/ (root)"
4. Нажмите кнопку "Save"
5. Страница перезагрузится
6. Появится сообщение: "Your site is live at https://USERNAME.github.io/sewing/"
7. Подождите 2-3 минуты для завершения развертывания

**Этап 5: Проверка работоспособности**
1. Откройте ссылку: https://USERNAME.github.io/sewing/
2. Должна открыться главная страница сайта
3. Проверьте админпанель: https://USERNAME.github.io/sewing/admin.html
4. Введите пароль: admin123
5. Убедитесь, что все функции работают

### 2.2 Netlify (бесплатно)

**Преимущества:**
- 100 GB месячного трафика
- Автоматический SSL
- Быстрый CDN
- Поддержка форм и serverless функций

**Инструкция:**

**Этап 1: Регистрация**
1. Перейдите на https://www.netlify.com
2. Нажмите "Sign up"
3. Выберите "Sign up with GitHub"
4. Авторизуйте приложение Netlify в GitHub

**Этап 2: Импорт проекта**
1. После входа нажмите "Add new site"
2. Выберите "Import an existing project"
3. Выберите "GitHub"
4. Найдите и выберите репозиторий "sewing"
5. Настройки сборки:
   ```
   Branch to deploy: main
   Build command: (оставить пустым)
   Publish directory: (оставить пустым или указать .)
   ```
6. Нажмите "Deploy site"

**Этап 3: Настройка имени**
1. После развертывания перейдите в "Site settings"
2. Найдите секцию "Site information"
3. Нажмите "Change site name"
4. Введите желаемое имя: например, "ulla-sewing"
5. Сайт станет доступен по адресу: https://ulla-sewing.netlify.app

### 2.3 Собственный VPS сервер

**Требования:**
- VPS с Ubuntu 20.04+
- Root доступ или sudo права
- Минимум 512 MB RAM
- Статический IP адрес

**Полная инструкция по настройке:**

**Этап 1: Подключение к серверу**
```bash
# Замените IP_ADDRESS на IP вашего сервера
ssh root@IP_ADDRESS

# Если используется не root, а другой пользователь:
ssh username@IP_ADDRESS
```

**Этап 2: Обновление системы**
```bash
# Обновление списка пакетов
sudo apt update

# Обновление установленных пакетов
sudo apt upgrade -y

# Установка базовых утилит
sudo apt install -y curl wget git nano ufw
```

**Этап 3: Установка Nginx**
```bash
# Установка веб-сервера Nginx
sudo apt install nginx -y

# Запуск Nginx
sudo systemctl start nginx

# Добавление в автозагрузку
sudo systemctl enable nginx

# Проверка статуса
sudo systemctl status nginx

# Если статус "active (running)" - все работает правильно
```

**Этап 4: Настройка Firewall**
```bash
# Разрешить SSH (чтобы не потерять доступ)
sudo ufw allow OpenSSH

# Разрешить HTTP
sudo ufw allow 'Nginx HTTP'

# Разрешить HTTPS
sudo ufw allow 'Nginx HTTPS'

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status
```

**Этап 5: Создание структуры каталогов**
```bash
# Создание директории для сайта
sudo mkdir -p /var/www/sewing

# Установка прав владения
sudo chown -R $USER:$USER /var/www/sewing

# Установка прав доступа
sudo chmod -R 755 /var/www/sewing

# Проверка создания
ls -la /var/www/
```

**Этап 6: Загрузка файлов сайта**

Способ A: С локального компьютера через SCP
```bash
# Выполните на вашем компьютере (не на сервере!)
# Замените путь и IP на свои
scp -r /path/to/sewing/files/* root@IP_ADDRESS:/var/www/sewing/
```

Способ B: Клонирование из GitHub
```bash
# На сервере выполните:
cd /var/www/sewing
git clone https://github.com/USERNAME/sewing.git .

# Точка в конце важна - она клонирует в текущую директорию
```

Способ C: Через FileZilla
1. Скачайте FileZilla: https://filezilla-project.org
2. Откройте FileZilla
3. Подключитесь к серверу:
   ```
   Хост: sftp://IP_ADDRESS
   Имя пользователя: root или ваш username
   Пароль: ваш пароль SSH
   Порт: 22
   ```
4. В правой панели перейдите в /var/www/sewing
5. В левой панели найдите файлы сайта
6. Выделите все файлы и перетащите в правую панель

**Этап 7: Настройка конфигурации Nginx**
```bash
# Создание файла конфигурации
sudo nano /etc/nginx/sites-available/sewing
```

Вставьте следующую конфигурацию:
```nginx
# Базовая конфигурация без защиты
server {
    listen 80;
    listen [::]:80;
    
    # Замените на ваш домен или IP
    server_name ваш-домен.com www.ваш-домен.com;
    
    # Корневая директория сайта
    root /var/www/sewing;
    
    # Индексный файл
    index index.html;
    
    # Логи доступа
    access_log /var/log/nginx/sewing-access.log;
    error_log /var/log/nginx/sewing-error.log;
    
    # Основная директива обработки запросов
    location / {
        try_files $uri $uri/ =404;
    }
    
    # Кеширование статических файлов
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Безопасность: скрыть версию Nginx
    server_tokens off;
    
    # Заголовки безопасности
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
```

Сохраните файл: Ctrl+X, затем Y, затем Enter

**Этап 8: Активация конфигурации**
```bash
# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/sewing /etc/nginx/sites-enabled/

# Удаление дефолтной конфигурации (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Тест конфигурации на ошибки
sudo nginx -t

# Если вывод содержит "syntax is ok" и "test is successful" - все хорошо

# Перезагрузка Nginx
sudo systemctl reload nginx
```

**Этап 9: Проверка работы**
1. Откройте браузер
2. Введите в адресной строке IP адрес вашего сервера или домен
3. Должна загрузиться главная страница сайта
4. Проверьте админпанель: http://ваш-ip/admin.html

---

## РАЗДЕЛ 3. БЕЗОПАСНОСТЬ АДМИНПАНЕЛИ

### 3.1 Анализ угроз

**Текущее состояние безопасности:**
⚠️ КРИТИЧНО: По умолчанию админпанель защищена только JavaScript проверкой пароля. Это НЕ является надежной защитой, так как:
- Пароль хранится в открытом виде в коде
- Любой может просмотреть исходный код (F12 в браузере)
- Нет защиты от брутфорс атак
- Нет логирования попыток входа

**Возможные угрозы:**
1. Несанкционированный доступ к админпанели
2. Изменение или удаление контента сайта
3. Кража данных из localStorage
4. DDoS атаки на админпанель
5. SQL инъекции (не применимо, но общий риск)
6. XSS атаки через формы

### 3.2 Уровень защиты №1: Изменение пароля

**Важность:** КРИТИЧЕСКАЯ  
**Сложность:** Низкая  
**Время выполнения:** 2 минуты

**Инструкция:**

1. Откройте файл admin.js в текстовом редакторе
2. Найдите строку (примерно строка 10-15):
   ```javascript
   if (password === 'admin123') {
   ```
3. Замените 'admin123' на ваш сложный пароль:
   ```javascript
   if (password === 'Ваш$СложныйПароль2024!@#') {
   ```
4. Сохраните файл
5. Загрузите измененный файл на сервер

**Требования к паролю:**
- Минимум 12 символов
- Заглавные и строчные буквы (A-Z, a-z)
- Цифры (0-9)
- Специальные символы (!@#$%^&*)
- Не использовать словарные слова
- Не использовать личную информацию
- Не использовать последовательности (123456, qwerty)

**Примеры надежных паролей:**
- `Zx9$Km2#Pq8@Lw5!`
- `MyS3cur3P@ssw0rd!2024`
- `!Qw3Rt5Yu7Io9!@#`

**Примеры СЛАБЫХ паролей (НЕ ИСПОЛЬЗУЙТЕ):**
- `password123`
- `admin`
- `12345678`
- `qwerty`
- имя+дата рождения

### 3.3 Уровень защиты №2: HTTP Basic Authentication

**Важность:** ВЫСОКАЯ  
**Сложность:** Средняя  
**Время выполнения:** 10-15 минут

HTTP Basic Authentication - это стандартный метод аутентификации на уровне веб-сервера. Браузер будет запрашивать логин и пароль ДО загрузки страницы.

**Для Nginx:**

**Шаг 1: Установка утилиты для создания паролей**
```bash
sudo apt update
sudo apt install apache2-utils -y
```

**Шаг 2: Создание файла с паролем**
```bash
# Создание файла .htpasswd с пользователем admin
sudo htpasswd -c /etc/nginx/.htpasswd admin

# Система попросит ввести пароль дважды
# Введите надежный пароль
```

**Проверка создания файла:**
```bash
# Просмотр содержимого
sudo cat /etc/nginx/.htpasswd

# Вывод будет примерно таким:
# admin:$apr1$abc123$xyz789encoded
# Пароль зашифрован - это правильно!
```

**Добавление дополнительных пользователей:**
```bash
# БЕЗ флага -c (чтобы не перезаписать файл)
sudo htpasswd /etc/nginx/.htpasswd user2
```

**Шаг 3: Изменение конфигурации Nginx**
```bash
sudo nano /etc/nginx/sites-available/sewing
```

Замените конфигурацию на следующую:
```nginx
server {
    listen 80;
    listen [::]:80;
    server_name ваш-домен.com www.ваш-домен.com;
    root /var/www/sewing;
    index index.html;
    
    # Обычные страницы доступны всем
    location / {
        try_files $uri $uri/ =404;
    }
    
    # ЗАЩИТА АДМИНПАНЕЛИ
    location = /admin.html {
        auth_basic "Административная панель - вход ограничен";
        auth_basic_user_file /etc/nginx/.htpasswd;
        try_files $uri =404;
    }
    
    # ЗАЩИТА VISUAL EDITOR
    location = /visual-editor.html {
        auth_basic "Административная панель - вход ограничен";
        auth_basic_user_file /etc/nginx/.htpasswd;
        try_files $uri =404;
    }
    
    # ЗАЩИТА JS ФАЙЛОВ АДМИНКИ
    location ~ ^/(admin\.js|visual-editor\.js|admin-styles\.css)$ {
        auth_basic "Административная панель - вход ограничен";
        auth_basic_user_file /etc/nginx/.htpasswd;
        try_files $uri =404;
    }
    
    # Статические файлы с кешированием
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

**Шаг 4: Применение изменений**
```bash
# Тест конфигурации
sudo nginx -t

# Если OK - перезагружаем
sudo systemctl reload nginx

# Проверка статуса
sudo systemctl status nginx
```

**Шаг 5: Тестирование**
1. Откройте http://ваш-домен.com/admin.html
2. Должно появиться окно ввода логина и пароля
3. Введите:
   - Username: admin
   - Password: ваш пароль из htpasswd
4. Если все настроено правильно - админпанель откроется

**Для Apache (.htaccess):**

**Шаг 1: Создание файла паролей**
```bash
htpasswd -c /var/www/.htpasswd admin
```

**Шаг 2: Создание .htaccess**
```bash
nano /var/www/sewing/.htaccess
```

Содержимое:
```apache
# Защита административных файлов
<FilesMatch "(admin\.html|visual-editor\.html|admin\.js|visual-editor\.js|admin-styles\.css)$">
    AuthType Basic
    AuthName "Административная панель"
    AuthUserFile /var/www/.htpasswd
    Require valid-user
</FilesMatch>
```

**Шаг 3: Включение .htaccess в Apache**
```bash
sudo nano /etc/apache2/sites-available/sewing.conf
```

Добавьте:
```apache
<Directory /var/www/sewing>
    AllowOverride All
    Require all granted
</Directory>
```

```bash
sudo a2enmod rewrite
sudo systemctl restart apache2
```

### 3.4 Уровень защиты №3: IP Whitelist

**Важность:** МАКСИМАЛЬНАЯ  
**Сложность:** Средняя  
**Время выполнения:** 5 минут

Ограничение доступа к админпанели только с определенных IP адресов - один из самых надежных методов защиты.

**Шаг 1: Определите ваш IP адрес**

Способ A: Через веб-сервис
1. Перейдите на https://whatismyipaddress.com
2. Скопируйте ваш IPv4 адрес (например: 203.0.113.45)

Способ B: Через командную строку
```bash
# На вашем компьютере (не на сервере!)
curl ifconfig.me
```

**Шаг 2: Настройка для Nginx**
```bash
sudo nano /etc/nginx/sites-available/sewing
```

Обновите секцию защиты:
```nginx
location /admin.html {
    # Разрешить доступ только с указанных IP
    allow 203.0.113.45;          # Ваш домашний IP
    allow 198.51.100.0/24;       # Диапазон IP вашей компании
    deny all;                     # Запретить всем остальным
    
    # Дополнительная защита паролем
    auth_basic "Административная панель";
    auth_basic_user_file /etc/nginx/.htpasswd;
    
    try_files $uri =404;
}

# То же самое для visual-editor
location /visual-editor.html {
    allow 203.0.113.45;
    allow 198.51.100.0/24;
    deny all;
    
    auth_basic "Административная панель";
    auth_basic_user_file /etc/nginx/.htpasswd;
    
    try_files $uri =404;
}
```

**Шаг 3: Применение**
```bash
sudo nginx -t
sudo systemctl reload nginx
```

**Для Apache:**
```apache
<FilesMatch "admin\.html">
    Order Deny,Allow
    Deny from all
    Allow from 203.0.113.45
    Allow from 198.51.100.0/24
</FilesMatch>
```

**Важно:**
- Динамические IP адреса могут меняться! Проверьте у провайдера.
- Для мобильного доступа добавьте IP вашего оператора связи.
- Используйте диапазоны IP (CIDR) для офисных сетей.

### 3.5 Уровень защиты №4: SSL/HTTPS

**Важность:** КРИТИЧЕСКАЯ  
**Сложность:** Низкая  
**Время выполнения:** 10 минут

HTTPS шифрует все данные между браузером и сервером, включая пароли.

**Установка Let's Encrypt (бесплатный SSL):**

```bash
# Шаг 1: Установка Certbot
sudo apt install certbot python3-certbot-nginx -y

# Шаг 2: Получение сертификата
# Замените на ваш реальный домен!
sudo certbot --nginx -d ваш-домен.com -d www.ваш-домен.com

# Certbot задаст вопросы:

# 1. Email для важных уведомлений:
# Введите: ваш@email.com

# 2. Согласие с условиями использования:
# Введите: Y (согласен)

# 3. Получать новости от EFF:
# Введите: N (не обязательно)

# 4. Перенаправлять ли HTTP на HTTPS:
# Введите: 2 (перенаправлять - рекомендуется)

# Certbot автоматически:
# - Получит SSL сертификат
# - Настроит Nginx
# - Включит перенаправление HTTP → HTTPS

# Шаг 3: Проверка автообновления
sudo certbot renew --dry-run

# Если команда выполнилась без ошибок - автообновление работает
```

**Проверка работы HTTPS:**
1. Откройте https://ваш-домен.com (обратите внимание на https)
2. В адресной строке должен быть значок замка 🔒
3. Нажмите на замок → "Соединение защищено"

**Ручное обновление сертификата (если нужно):**
```bash
sudo certbot renew
```

### 3.6 Уровень защиты №5: Fail2Ban

**Важность:** ВЫСОКАЯ  
**Сложность:** Средняя  
**Время выполнения:** 15 минут

Fail2Ban автоматически блокирует IP адреса после нескольких неудачных попыток входа.

**Установка и настройка:**

**Шаг 1: Установка**
```bash
sudo apt install fail2ban -y
```

**Шаг 2: Создание фильтра для Nginx**
```bash
sudo nano /etc/fail2ban/filter.d/nginx-auth.conf
```

Вставьте:
```ini
[Definition]
failregex = ^ \[error\] \d+#\d+: \*\d+ user "\S+":? (password mismatch|was not found in), client: <HOST>, server: \S+, request: "\S+ \S+ HTTP/\d+\.\d+", host: "\S+"
            ^ \[error\] \d+#\d+: \*\d+ no user/password was provided for basic authentication, client: <HOST>, server: \S+, request: "\S+ \S+ HTTP/\d+\.\d+", host: "\S+"
ignoreregex =
```

**Шаг 3: Создание jail (правила блокировки)**
```bash
sudo nano /etc/fail2ban/jail.local
```

Вставьте:
```ini
[DEFAULT]
# Ban time: 1 час (3600 секунд)
bantime = 3600

# Find time: окно в 10 минут
findtime = 600

# Max retry: максимум 3 попытки
maxretry = 3

[nginx-auth]
enabled = true
port = http,https
logpath = /var/log/nginx/error.log
maxretry = 3
bantime = 3600
findtime = 600

[nginx-noscript]
enabled = true
port = http,https
filter = nginx-noscript
logpath = /var/log/nginx/access.log
maxretry = 6

[nginx-badbots]
enabled = true
port = http,https
filter = nginx-badbots
logpath = /var/log/nginx/access.log
maxretry = 2

[nginx-noproxy]
enabled = true
port = http,https
filter = nginx-noproxy
logpath = /var/log/nginx/access.log
maxretry = 2
```

**Шаг 4: Запуск Fail2Ban**
```bash
# Запуск службы
sudo systemctl start fail2ban

# Добавление в автозагрузку
sudo systemctl enable fail2ban

# Проверка статуса
sudo systemctl status fail2ban
```

**Шаг 5: Проверка работы**
```bash
# Просмотр активных jail
sudo fail2ban-client status

# Детальная информация по nginx-auth
sudo fail2ban-client status nginx-auth

# Просмотр забаненных IP
sudo fail2ban-client get nginx-auth banip
```

**Полезные команды:**
```bash
# Разбанить IP вручную
sudo fail2ban-client set nginx-auth unbanip IP_ADDRESS

# Добавить IP в белый список (не банить)
sudo nano /etc/fail2ban/jail.local
# Добавьте под [DEFAULT]:
# ignoreip = 127.0.0.1 203.0.113.45
```

### 3.7 Уровень защиты №6: Rate Limiting

**Важность:** СРЕДНЯЯ  
**Сложность:** Низкая  
**Время выполнения:** 5 минут

Ограничение количества запросов к админпанели в единицу времени.

**Для Nginx:**
```bash
sudo nano /etc/nginx/nginx.conf
```

Добавьте в секцию `http`:
```nginx
http {
    # ... другие настройки ...
    
    # Создание зоны ограничений
    # 10m = 10 мегабайт памяти (примерно 160,000 IP адресов)
    # rate=5r/m = 5 запросов в минуту
    limit_req_zone $binary_remote_addr zone=adminlimit:10m rate=5r/m;
    
    # ... остальные настройки ...
}
```

Обновите конфигурацию сайта:
```bash
sudo nano /etc/nginx/sites-available/sewing
```

Добавьте в location админки:
```nginx
location /admin.html {
    # Применяем ограничение
    limit_req zone=adminlimit burst=2 nodelay;
    
    # Остальные настройки защиты
    allow 203.0.113.45;
    deny all;
    auth_basic "Административная панель";
    auth_basic_user_file /etc/nginx/.htpasswd;
    try_files $uri =404;
}
```

Параметры:
- `burst=2` - разрешить "всплеск" до 2 дополнительных запросов
- `nodelay` - не задерживать разрешенные запросы

```bash
# Применить изменения
sudo nginx -t
sudo systemctl reload nginx
```

### 3.8 Уровень защиты №7: Дополнительные заголовки безопасности

**Важность:** СРЕДНЯЯ  
**Сложность:** Низкая  
**Время выполнения:** 5 минут

**Для Nginx:**
```bash
sudo nano /etc/nginx/sites-available/sewing
```

Добавьте заголовки безопасности:
```nginx
server {
    # ... другие настройки ...
    
    location / {
        # Скрыть версию Nginx
        server_tokens off;
        
        # Content Security Policy
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://unpkg.com https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://libretranslate.com; style-src 'self' 'unsafe-inline' https://unpkg.com https://cdnjs.cloudflare.com; img-src 'self' data: https:; font-src 'self' data: https://cdnjs.cloudflare.com; connect-src 'self' https://libretranslate.com;" always;
        
        # Запретить открытие в iframe
        add_header X-Frame-Options "SAMEORIGIN" always;
        
        # Запретить MIME-sniffing
        add_header X-Content-Type-Options "nosniff" always;
        
        # Включить XSS фильтр браузера
        add_header X-XSS-Protection "1; mode=block" always;
        
        # Политика Referrer
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        
        # Permissions Policy
        add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
        
        try_files $uri $uri/ =404;
    }
}
```

```bash
sudo nginx -t
sudo systemctl reload nginx
```

**Проверка заголовков:**
```bash
curl -I https://ваш-домен.com
```

### 3.9 Чеклист безопасности

Используйте этот чеклист для проверки безопасности:

```
БАЗОВАЯ БЕЗОПАСНОСТЬ:
☐ Изменен стандартный пароль admin123
☐ Используется надежный пароль (12+ символов)
☐ Настроен HTTP Basic Authentication
☐ Установлен SSL сертификат (HTTPS)
☐ Настроено автообновление SSL сертификата

СЕРВЕРНАЯ БЕЗОПАСНОСТЬ:
☐ Система обновлена (apt update && apt upgrade)
☐ Настроен UFW firewall
☐ Открыты только необходимые порты (22, 80, 443)
☐ Отключен root login по SSH (рекомендуется)
☐ Настроена аутентификация SSH по ключам

ЗАЩИТА АДМИНПАНЕЛИ:
☐ Включен IP Whitelist (если возможно)
☐ Настроен Fail2Ban
☐ Включен Rate Limiting
☐ Добавлены security headers
☐ Скрыта версия веб-сервера

МОНИТОРИНГ:
☐ Регулярно проверяются логи (/var/log/nginx/)
☐ Настроены email уведомления о подозрительной активности
☐ Проверяется список забаненных IP в Fail2Ban

РЕЗЕРВНОЕ КОПИРОВАНИЕ:
☐ Настроено регулярное резервное копирование (еженедельно)
☐ Резервные копии хранятся вне сервера
☐ Проверена процедура восстановления из бэкапа

ДОПОЛНИТЕЛЬНО:
☐ Изменено имя файла админпанели (admin.html → secret-xyz.html)
☐ Настроен VPN для доступа к админпанели (для корпоративного использования)
☐ Включено логирование всех действий в админпанели
☐ Настроена двухфакторная аутентификация (если поддерживается)
```

---

## РАЗДЕЛ 4. МОНИТОРИНГ И ЛОГИРОВАНИЕ

### 4.1 Просмотр логов

**Логи доступа Nginx:**
```bash
# Последние 50 строк
sudo tail -50 /var/log/nginx/access.log

# Мониторинг в реальном времени
sudo tail -f /var/log/nginx/access.log

# Поиск по IP
sudo grep "203.0.113.45" /var/log/nginx/access.log

# Подсчет запросов к админпанели
sudo grep "admin.html" /var/log/nginx/access.log | wc -l
```

**Логи ошибок Nginx:**
```bash
# Последние 50 ошибок
sudo tail -50 /var/log/nginx/error.log

# Неудачные попытки аутентификации
sudo grep "password" /var/log/nginx/error.log

# Ошибки за последний час
sudo grep "$(date -d '1 hour ago' +'%d/%b/%Y:%H')" /var/log/nginx/error.log
```

**Статистика по IP адресам:**
```bash
# Топ 10 IP адресов по количеству запросов
sudo awk '{print $1}' /var/log/nginx/access.log | sort | uniq -c | sort -rn | head -10

# Уникальные посетители за день
sudo grep "$(date +'%d/%b/%Y')" /var/log/nginx/access.log | awk '{print $1}' | sort -u | wc -l
```

### 4.2 Автоматический мониторинг

Создайте скрипт для автоматической проверки безопасности:

```bash
sudo nano /usr/local/bin/security-monitor.sh
```

Содержимое:
```bash
#!/bin/bash

# Настройки
LOGFILE="/var/log/nginx/error.log"
ACCESSLOG="/var/log/nginx/access.log"
EMAIL="ваш@email.com"
THRESHOLD=10
DATE=$(date +'%Y-%m-%d %H:%M:%S')

# Подсчет неудачных попыток входа за последний час
FAILED_AUTH=$(grep "$(date -d '1 hour ago' +'%d/%b/%Y:%H')" $LOGFILE | grep -c "password mismatch")

# Подсчет попыток доступа к админпанели
ADMIN_ATTEMPTS=$(grep "$(date -d '1 hour ago' +'%d/%b/%Y:%H')" $ACCESSLOG | grep -c "admin.html")

# Отправка уведомления при превышении порога
if [ $FAILED_AUTH -gt $THRESHOLD ]; then
    echo "⚠️ SECURITY ALERT ⚠️
    
Время: $DATE
Обнаружено: $FAILED_AUTH неудачных попыток входа в админпанель за последний час!

Рекомендуется проверить:
1. Логи: sudo tail -50 /var/log/nginx/error.log
2. Заблокированные IP: sudo fail2ban-client status nginx-auth
3. Активные сессии: sudo who

Топ 5 IP с неудачными попытками:
$(grep 'password mismatch' $LOGFILE | awk '{print $11}' | sort | uniq -c | sort -rn | head -5)
" | mail -s "⚠️ Security Alert: $FAILED_AUTH неудачных входов" $EMAIL
fi

# Лог работы скрипта
echo "$DATE - Failed auth: $FAILED_AUTH, Admin attempts: $ADMIN_ATTEMPTS" >> /var/log/security-monitor.log
```

Сделайте скрипт исполняемым:
```bash
sudo chmod +x /usr/local/bin/security-monitor.sh
```

Добавьте в cron (запуск каждый час):
```bash
crontab -e
```

Добавьте строку:
```
0 * * * * /usr/local/bin/security-monitor.sh
```

### 4.3 Настройка email уведомлений

```bash
# Установка почтового клиента
sudo apt install mailutils postfix -y

# При установке выберите:
# General type: Internet Site
# System mail name: ваш-домен.com

# Тест отправки
echo "Тестовое сообщение" | mail -s "Test" ваш@email.com
```

---

## РАЗДЕЛ 5. РЕЗЕРВНОЕ КОПИРОВАНИЕ

### 5.1 Через админпанель (рекомендуется)

**Экспорт данных:**
1. Откройте админпанель: https://ваш-сайт.com/admin.html
2. Войдите с паролем
3. Перейдите во вкладку **⚙️ Настройки**
4. Прокрутите до раздела "Резервное копирование"
5. Нажмите кнопку **📥 Экспорт данных**
6. Файл `sewing-backup-YYYY-MM-DD.json` будет загружен на ваш компьютер
7. Сохраните файл в безопасном месте

**Рекомендуемый график:**
- Ежедневно при активных изменениях
- Еженедельно при редких обновлениях
- Перед любыми значительными изменениями

**Восстановление данных:**
1. Откройте админпанель
2. Перейдите в **⚙️ Настройки**
3. Найдите раздел "Резервное копирование"
4. Нажмите **📤 Импорт данных**
5. Выберите файл бэкапа
6. Подтвердите импорт
7. Страница автоматически перезагрузится

### 5.2 Резервное копирование на сервере

**Создание скрипта автоматического бэкапа:**

```bash
sudo nano /usr/local/bin/backup-website.sh
```

Содержимое:
```bash
#!/bin/bash

# Настройки
WEBSITE_DIR="/var/www/sewing"
BACKUP_DIR="/var/backups/sewing"
DATE=$(date +%Y%m%d_%H%M%S)
RETENTION_DAYS=30

# Создание директории для бэкапов
mkdir -p $BACKUP_DIR

# Создание архива
tar -czf $BACKUP_DIR/sewing-backup-$DATE.tar.gz -C $WEBSITE_DIR .

# Удаление старых бэкапов (старше 30 дней)
find $BACKUP_DIR -name "sewing-backup-*.tar.gz" -mtime +$RETENTION_DAYS -delete

# Подсчет размера бэкапа
SIZE=$(du -h $BACKUP_DIR/sewing-backup-$DATE.tar.gz | cut -f1)

# Лог
echo "$DATE - Backup created: sewing-backup-$DATE.tar.gz ($SIZE)" >> /var/log/backup.log

# Опционально: отправка на удаленный сервер
# scp $BACKUP_DIR/sewing-backup-$DATE.tar.gz user@backup-server:/backups/
```

Сделайте исполняемым:
```bash
sudo chmod +x /usr/local/bin/backup-website.sh
```

Добавьте в cron (ежедневно в 3:00 ночи):
```bash
sudo crontab -e
```

Добавьте:
```
0 3 * * * /usr/local/bin/backup-website.sh
```

**Восстановление из бэкапа:**
```bash
# Просмотр доступных бэкапов
ls -lh /var/backups/sewing/

# Восстановление
sudo tar -xzf /var/backups/sewing/sewing-backup-20260111_030000.tar.gz -C /var/www/sewing/
```

---

## РАЗДЕЛ 6. ОБНОВЛЕНИЕ И ПОДДЕРЖКА

### 6.1 Обновление файлов сайта

**Через Git (если используется):**
```bash
cd /var/www/sewing
git pull origin main
sudo systemctl reload nginx
```

**Через FTP/FileZilla:**
1. Подключитесь к серверу
2. Загрузите обновленные файлы
3. Перезагрузите Nginx

### 6.2 Обновление системы

```bash
# Обновление списка пакетов
sudo apt update

# Обновление установленных пакетов
sudo apt upgrade -y

# Обновление ядра и безопасности
sudo apt dist-upgrade -y

# Удаление неиспользуемых пакетов
sudo apt autoremove -y

# Очистка кеша
sudo apt clean

# Перезагрузка (если требуется)
sudo reboot
```

Рекомендуется выполнять еженедельно.

---

## РАЗДЕЛ 7. РЕШЕНИЕ ПРОБЛЕМ

### 7.1 Сайт не открывается

**Проверка 1: Работает ли Nginx?**
```bash
sudo systemctl status nginx
```

Если не работает:
```bash
sudo systemctl start nginx
```

**Проверка 2: Правильная ли конфигурация?**
```bash
sudo nginx -t
```

Если есть ошибки - исправьте их в конфигурации.

**Проверка 3: Открыты ли порты в firewall?**
```bash
sudo ufw status
```

Если порты закрыты:
```bash
sudo ufw allow 'Nginx Full'
```

**Проверка 4: Доступен ли сервер?**
```bash
ping ваш-домен.com
```

### 7.2 Админпанель не сохраняет данные

**Причина 1:** Режим "Инкогнито" в браузере
- localStorage не работает в режиме инкогнито
- Используйте обычный режим браузера

**Причина 2:** Отключены cookies в браузере
- Проверьте настройки браузера
- Разрешите cookies для вашего домена

**Причина 3:** Нехватка места в localStorage
- localStorage имеет лимит ~10 MB
- Экспортируйте и очистите старые данные
- Оптимизируйте изображения (используйте ссылки вместо base64)

### 7.3 Забыт пароль от админпанели

**Решение 1:** Изменить в файле admin.js
1. Подключитесь к серверу
2. Откройте файл:
   ```bash
   nano /var/www/sewing/admin.js
   ```
3. Найдите строку с проверкой пароля
4. Измените пароль
5. Сохраните файл

**Решение 2:** Сбросить HTTP Basic Auth
```bash
# Создайте новый пароль
sudo htpasswd /etc/nginx/.htpasswd admin
```

### 7.4 SSL сертификат не обновляется

```bash
# Проверка статуса certbot
sudo systemctl status certbot.timer

# Попытка ручного обновления
sudo certbot renew --dry-run

# Если есть ошибки - проверьте логи
sudo cat /var/log/letsencrypt/letsencrypt.log
```

---

## РАЗДЕЛ 8. КОНТАКТЫ И ПОДДЕРЖКА

### 8.1 Полезные ресурсы

**Официальная документация:**
- Nginx: https://nginx.org/ru/docs/
- Let's Encrypt: https://letsencrypt.org/docs/
- Fail2Ban: https://www.fail2ban.org/wiki/

**Онлайн инструменты:**
- SSL тест: https://www.ssllabs.com/ssltest/
- DNS проверка: https://dnschecker.org
- Security headers: https://securityheaders.com
- Проверка IP: https://whatismyipaddress.com

**Форумы и сообщества:**
- Stack Overflow: https://stackoverflow.com
- Server Fault: https://serverfault.com
- Reddit /r/selfhosted: https://reddit.com/r/selfhosted

### 8.2 Чеклист перед запуском

Перед запуском сайта в продакшн проверьте:

```
ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:
☐ Сайт протестирован в Chrome, Firefox, Safari
☐ Сайт адаптивен (mobile, tablet, desktop)
☐ Все ссылки работают
☐ Изображения загружаются
☐ Формы работают корректно
☐ Админпанель функционирует

БЕЗОПАСНОСТЬ:
☐ Изменен стандартный пароль
☐ Настроен HTTPS
☐ Настроена базовая аутентификация
☐ Включен Fail2Ban
☐ Настроены security headers
☐ Проведен тест на безопасность

ПРОИЗВОДИТЕЛЬНОСТЬ:
☐ Изображения оптимизированы
☐ Включено кеширование
☐ Включен gzip сжатие
☐ Минимизированы CSS/JS файлы (опционально)

SEO И МЕТАДАННЫЕ:
☐ Настроен robots.txt
☐ Добавлены meta теги
☐ Настроен sitemap.xml (опционально)
☐ Добавлена аналитика (Google Analytics) (опционально)

РЕЗЕРВИРОВАНИЕ:
☐ Настроено автоматическое резервное копирование
☐ Проверена процедура восстановления
☐ Бэкапы хранятся вне основного сервера

МОНИТОРИНГ:
☐ Настроены email уведомления
☐ Проверяются логи регулярно
☐ Настроен мониторинг uptime (опционально)
```

---

## ЗАКЛЮЧЕНИЕ

Данное руководство содержит полную информацию по развертыванию и обеспечению безопасности сайта швейной мастерской. Следуя инструкциям, вы сможете:

1. Развернуть сайт на любом из предложенных хостингов
2. Настроить надежную защиту админпанели
3. Обеспечить безопасность данных
4. Настроить мониторинг и резервное копирование
5. Решить типичные проблемы

**Важно помнить:**
- Безопасность - это процесс, а не разовое действие
- Регулярно обновляйте систему и проверяйте логи
- Делайте резервные копии перед любыми изменениями
- Используйте надежные пароли
- Не пренебрегайте многоуровневой защитой

**Рекомендуемый минимум безопасности:**
1. Изменить стандартный пароль
2. Настроить HTTP Basic Authentication
3. Включить HTTPS
4. Настроить Fail2Ban
5. Регулярное резервное копирование

При возникновении вопросов обращайтесь к официальной документации используемых технологий или профильным форумам.

**Удачи с вашим проектом!**

---

*Конец документа*
